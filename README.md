.vim
====

VIM configuration

```
sudo apt-get install exuberant-ctags
sudo apt-get install php5 php-pear
sudo pear install PHP_CodeSniffer
git clone https://git.gloversure.co.uk/gloversure/internal/vim.git .vim
cd .vim 
git submodule init
git submodule update
```
